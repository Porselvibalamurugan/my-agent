export declare function initMonacoTheme(): void;
/** Create the single editor instance (call once when the first tab opens). */
export declare function createEditorInstance(container: HTMLElement): void;
/** Create or reuse a model for a file, then switch the editor to it. */
export declare function openFileModel(filePath: string, filename: string, content: string): void;
/** Get the current content of a file's model. */
export declare function getModelContent(filePath: string): string;
/** Dispose a single file model (when closing a tab). */
export declare function closeFileModel(filePath: string): void;
/** Dispose the editor instance and all models. */
export declare function disposeAll(): void;
